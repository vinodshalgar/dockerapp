pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'hello'
                // sh 'make' 
                // archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true 
            }
        }

         stage('Build1') {
            steps {
                echo 'hello'
                // sh 'make' 
                // archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true 
            }
        }
    }
}